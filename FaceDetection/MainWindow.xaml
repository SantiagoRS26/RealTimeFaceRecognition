<Window x:Class="FaceDetection.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FaceDetection"
        mc:Ignorable="d"
        Title="Face Detection" Height="450" Width="800">
    <Grid>
        <Image Source="{Binding CurrentFrame}" Stretch="Uniform"/>

        <Border Background="Black" Opacity="0.5" CornerRadius="5" Padding="10"
                HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Personas Detectadas: " 
                           Foreground="White" FontSize="16" FontWeight="Bold"/>
                <TextBlock Text="{Binding CurrentPersonCount}" 
                           Foreground="Yellow" FontSize="16" FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
